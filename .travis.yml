git:
  depth: 3
install:
  - sudo apt-get -qq update
  - sudo apt-get install -y mksh qemu
matrix:
  include:
  - env: ARCH=alpha
  - env: ARCH=armv7
  - env: ARCH=crisv10
script:
  - ./embedded-test.sh --arch=$ARCH --libc=uclibc-ng
